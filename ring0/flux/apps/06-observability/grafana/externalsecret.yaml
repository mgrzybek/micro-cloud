# OIDC credentials and endpoints for the Grafana <-> Authentik integration,
# stored in OpenBao at secret/grafana. issuer-url and root-url are kept there
# rather than in Git because both embed the dynamic ts_suffix.
#
# The eso AppRole used by the ClusterSecretStore is read-only: push the secret
# using the root token instead, from inside the pki Incus instance
# (see core-services/pki/README.md#openbao):
#   bao kv put secret/grafana \
#     client-id=... client-secret=... \
#     issuer-url=https://idp.<ts_suffix>/application/o/grafana \
#     root-url=https://grafana.<ts_suffix>
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: grafana-oauth
  namespace: observability
spec:
  refreshInterval: 1h
  secretStoreRef:
    kind: ClusterSecretStore
    name: openbao
  target:
    name: grafana-oauth
  data:
    - secretKey: client-id
      remoteRef:
        key: grafana
        property: client-id
    - secretKey: client-secret
      remoteRef:
        key: grafana
        property: client-secret
    - secretKey: issuer-url
      remoteRef:
        key: grafana
        property: issuer-url
    - secretKey: root-url
      remoteRef:
        key: grafana
        property: root-url

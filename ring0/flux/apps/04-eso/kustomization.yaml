apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - helmrelease.yaml
  # cluster-secret-store.yaml is NOT managed by Flux: the ClusterSecretStore
  # references the OpenBao URL, CA bundle and AppRole credentials.
  # task flux creates it imperatively from dist/ artifacts.

apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - helmrelease.yaml
  # cluster-issuer.yaml is NOT managed by Flux: the ClusterIssuer references
  # the OpenBao URL and CA bundle which are environment-specific secrets.
  # task flux creates it imperatively from dist/ artifacts.
